RAC dg切换
——————————————————————————————————
1.告警工单>>实例告警
2.多实例>>>>实例状态不对
尝试打开数据库，启动不起来。
3.节点2 重启，30分钟解决不了，上报保监会。
4.切换dataguard 
主库rac>>停掉集群资源
DGrac>>取消recover，在两个节点分别激活四个实例，激活后，重启验证成功后，关闭数据库。
VIP 开始切换，停掉数据库监听，停VIP资源，修改/etc/hosts（主库注释掉vip） 修改vip,启动VIP资源，修改监听端口，启动监听。启动实例。应用验证。




1.接到数据库实例告警，检查数据库实例状态，发现rac节点一3个数据库实例（p0910301/p0910501/p0920401）状态异常，rac节点二服务器宕机。通知应用负责人杨敏华老师。
2.在节点一尝试打开数据库，发现后台日志报错：控制文件坏块错误。无法打开数据库到open状态。
3.切换dataguard：
主库：rac节点一，关闭正常的数据库p0920301，关闭rac资源。
DG库：检查数据库数据同步状态，取消日志应用，激活DG库，数据库启停后，验证数据库状态正常。关闭数据库。
DG库切换vip:停掉数据库监听，检查vip资源状态信息，停vip资源，修改/etc/hosts(主库注释掉vip),修改rac VIP资源。启动VIP资源，检查vip资源状态信息。修改监听端口，启动监听。启动实例，通知应用验证。检查数据库参数。
